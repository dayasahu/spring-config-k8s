apiVersion: v1
kind: ConfigMap
metadata:
  name: employee-config
data:
  application.yml: |
    spring:
      application:
        name: employee-service
      profiles:
        active: prod
    server:
      port: 8080
    logging:
      level:
        root: INFO
